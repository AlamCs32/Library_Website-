const { Book } = require('./book')
const { User } = require('./user')
const { User_Book } = require('./user_book')
const { UserInfo } = require('./user_info')

module.exports = {
    Book, User, UserInfo, User_Book
}